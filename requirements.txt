# 핵심 (합성 데이터로 학습·백테스트만 하려면 이것만 있으면 됨)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
python-dotenv>=1.0      # .env 파일에서 API 키 로드

# 실제 데이터 소스 (선택 — 필요할 때만 설치)
# Alpaca 실시간 데이터 + 페이퍼 트레이딩
alpaca-py>=0.20

# Massive.com 실제 거래소 데이터 (사용 시 주석 해제)
# massive
